// Defines styles as a Typescript objects (because MUI hates CSS)
const styles = {
  heading: {
    display: 'flex',
    justifyContent: 'center',
    margin: '20px',
    fontFamily: 'InterBold',
  },
};

export default styles;
